{
    title => 'Catalog',
    secrets => ['TopSecret'],
    password => 'playground',
    mongouri => 'mongodb://localhost',
    database => 'catalog',
    minion_mongo_database => 'minionmongo',
    hypnotoad => {
        listen => ['http://0.0.0.0:3100'],
        proxy => 1
    }
}
